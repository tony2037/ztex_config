#!/usr/bin/env bash
# Use this file for your own personal container image configuration, i.e., install packages.
# Any commands you list in this file are run as root (so avoid `sudo` in your commands) during the
# image build process. The example line below installs git and the build-essential package
# collection.

#apt-get install -y git build-essential

echo "[ZTEX]: {Ready to go}"
